<!-- Scaffolding generated by Casein v5.0.0.0 -->

<h2>View bio</h2>

<%= form_for @bio, :url => casein_bio_path(@bio) do |f| %>

	<%= render :partial => 'casein/bios/form', :locals => { :f => f } %>
	
	<p class="submits">
	  	<%= link_to 'Cancel', casein_bios_path, :class => 'btn btn-mini btn-danger' %>
		<%= f.submit "Save changes", :class => "btn btn-mini btn-primary" %>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "th-list"}Back to list".html_safe, casein_bios_path %></li>
<% end %>