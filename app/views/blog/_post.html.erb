<div class="row wrapper" id="portfolio-section">
  <div class="span12">
    <div class="section">
      <div class="box">

        <div class="title">
          <% if action_name == 'index' %>
            <%= link_to markdown(post.title), 
                        post_path(post.blog_category, post) %>
          <% else %>
            <%= markdown(post.title) %>
          <% end %>
        </div>

        <div class="metadata">
          Posted on 
          <%= post.date_posted.strftime("%b #{post.date_posted.day.ordinalize} %Y at %R") %> 
          under 
          <%= link_to post.blog_category.title,
                      post_category_path(post.blog_category) %>
        </div>

        <div class="content">
          <%= markdown(post.content) %>
        </div>

      </div>
    </div>
  </div>
</div>